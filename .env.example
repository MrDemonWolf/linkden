# Cloudflare
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=

# App URLs / Custom Domains
# NEXT_PUBLIC_SITE_URL — The public URL of the web app (used for SEO, sitemap, OG tags)
# NEXT_PUBLIC_API_URL  — The public URL of the API worker (used by the web app for tRPC calls)
# APP_URL              — Same as NEXT_PUBLIC_API_URL (used internally by the API worker)
# CORS_ORIGIN          — Must match NEXT_PUBLIC_SITE_URL (allows the web app to call the API)
#
# For local development:
NEXT_PUBLIC_SITE_URL=http://localhost:3001
NEXT_PUBLIC_API_URL=http://localhost:3000
APP_URL=http://localhost:3000
CORS_ORIGIN=http://localhost:3001
#
# For production with custom domains, set these to your actual domains:
# NEXT_PUBLIC_SITE_URL=https://links.yourdomain.com
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com
# APP_URL=https://api.yourdomain.com
# CORS_ORIGIN=https://links.yourdomain.com

# Authentication (choose one or both)
# Option 1: Cloudflare Access (recommended for Cloudflare deployments)
# Set your Cloudflare Access team domain (e.g., "mycompany" for mycompany.cloudflareaccess.com)
CF_ACCESS_TEAM_DOMAIN=

# Option 2: Clerk (optional backup auth)
# Get keys from https://dashboard.clerk.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Apple Wallet (optional)
APPLE_PASS_SIGNER_CERT=
APPLE_PASS_SIGNER_KEY=
APPLE_PASS_TYPE_ID=
APPLE_TEAM_ID=

# Resend Email (optional)
RESEND_API_KEY=

# CAPTCHA (optional — configure provider and keys in admin settings)
# Supports Cloudflare Turnstile or Google reCAPTCHA v3
